/** @type {import('next').NextConfig} */
const path = require('path');

const nextConfig = {
  // experimental: {
  //   serverActions: true,
  // },
  images: {
    domains: ['127.0.0.1'],
  },
}

module.exports = nextConfig
